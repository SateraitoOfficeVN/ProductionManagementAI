# src

Backend (`.NET 10` + EF Core, `backend/`) and frontend (Vite + React + TypeScript + Tailwind CSS v4, `frontend/`) application source. WI-001's skeleton and auth foundation are implemented and merged to `master`; see `ai/project.md` for verified build/run commands.
