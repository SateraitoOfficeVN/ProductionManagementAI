# 02-detailed-design

Create screen A DD from BD. Show processing, validation and pending DB/API reconciliation.

Prerequisites: agreed screen A, approved work item plan and relevant artifacts. Screen A is confirmed as production-order create/edit (WI-002; roadmap locked in `ai/project.md`).

Recorded runs: WI-002's first design pass (2026-09-16) was recorded as `WI-002/recording.md` plus `WI-002/ScreenA_DD_step.mp4`, but that run predates WI-002's restart against the rewritten templates. The files are kept locally by the project owner and not committed (videos are gitignored). The restarted run's artifacts and record are in `work-items/WI-002/` and `docs/en/`.
