# 03-database-design

Create screen A DB design. Show ERD, constraints and reconciliation with DD/API.

Prerequisites: agreed screen A, approved work item plan and relevant artifacts. Screen A is confirmed as production-order create/edit (WI-002; roadmap locked in `ai/project.md`).

Recorded runs, one subfolder per work item: [WI-002](WI-002/recording.md) (video: [`ScreenA_DB_step.mp4`](WI-002/ScreenA_DB_step.mp4)). This step ran before 02-detailed-design in WI-002's own execution order (BD → DB → DD, per `work-items/WI-002/plan.md`), rather than in this directory's numeric order.
